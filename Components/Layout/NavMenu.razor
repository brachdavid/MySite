<nav id="navbar" class="navbar navbar-expand-lg py-3 fixed-top position-absolute bg-black lead" data-bs-theme="dark">
    <div class="container-fluid col-xl-11 col-xxl-10">
        <a class="navbar-brand d-flex align-items-center" href="/" @onclick="@(async () => NavigationManager.NavigateTo("/", forceLoad: true))" Match="NavLinkMatch.All">
            <img src="https://davidbrach.cz/images/brand/logoNavMenu.webp" alt="Logo" loanding="lazy" class="nav-logo">
        </a>

        <button class="navbar-toggler" type="button" @onclick="ToggleMenu" aria-controls="navbarNav" aria-expanded="@IsMenuOpen" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse @(IsMenuOpen ? "show" : "")" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center gap-lg-2">
                <li class="nav-item">
                    <NavLink class="nav-link" href="/" @onclick="@(async () => NavigationManager.NavigateTo("/", forceLoad: true))" Match="NavLinkMatch.All">Úvod</NavLink>
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link" href="/o-mne" @onclick="@(async () => NavigationManager.NavigateTo("/o-mne", forceLoad: true))">O mně</NavLink>
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link" href="/prubeh-spoluprace" @onclick="@(async () => NavigationManager.NavigateTo("/prubeh-spoluprace", forceLoad: true))">Průběh spolupráce</NavLink>
                </li>
                <li class="nav-item">
                    <NavLink href="/reference" class="nav-link" @onclick="@(async () => NavigationManager.NavigateTo("/reference", forceLoad: true))">Reference</NavLink>
                </li>
                <li class="nav-item">
                    <NavLink href="/blog" class="nav-link" @onclick="@(async () => NavigationManager.NavigateTo("/blog", forceLoad: true))">Blog</NavLink>

                </li>
                <AuthorizeView>
                    <Authorized>
                        <li class="nav-item">
                            <NavLink class="nav-link" href="/admin" @onclick="@(async () => NavigationManager.NavigateTo("/admin", forceLoad: true))">Admin</NavLink>
                        </li>
                        <li class="nav-item">
                            <NavLink class="nav-link" href="/odhlaseni" @onclick="@(async () => NavigationManager.NavigateTo("/odhlaseni", forceLoad: true))">Odhlásit</NavLink>
                        </li>
                    </Authorized>
                </AuthorizeView>
                <li class="nav-item">
                    <NavLink class="btn btn-primary rounded-pill fs-5 py-1 px-4" href="/kontakt" @onclick="@(async () => NavigationManager.NavigateTo("/kontakt", forceLoad: true))">Kontakt</NavLink>
                </li>
            </ul>
        </div>
    </div>
</nav>
<style>
    .nav-logo {
        height: 2.7rem; /* Přizpůsobení výšce textu */
        max-height: 100%;
        width: auto; /* Automatické přizpůsobení šířky */
    }
</style>

@code {
    private bool IsMenuOpen = false;

    private void ToggleMenu()
    {
        IsMenuOpen = !IsMenuOpen;
    }
}
